# Django full stack web app

A full-stack Django web app for a basic content management system.

## Get Started

Install the python dependencies using the *requirements.txt*.

```
git clone https://github.com/alexberndt/django-fullstack.git
cd django-fullstack 
python manage.py runserver

```
Open the app in the browser at *localhost:8000*